# @autorent/mobile

Placeholder. The Expo app is built in **Phase 3**: Expo Router, NativeWind
consuming `@autorent/tokens`, i18n from `@autorent/i18n`, auth via the Better
Auth Expo plugin, and the browse → book → pay flow using the Stripe Payment
Sheet. It will call the shared `/api/v1` backend — never reimplementing business
rules.
